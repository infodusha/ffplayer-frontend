apps:
  - script: server.js
    name: frontend
    env:
      PORT: 3000
      NODE_ENV: development
    env_production:
      PORT: 3000
      NODE_ENV: production
deploy:
  production:
    key: E:/keys/ffplayer_rsa
    user: root
    host:
      - ffplayer.pro
    ssh_options: StrictHostKeyChecking=no
    ref: origin/master
    repo: git@github.com:infodusha/ffplayer-frontend.git
    path: /root/frontend
    pre-setup: apt-get install git -y; ls -la
    post-setup: ls -la
    pre-deploy-local: echo 'This is a local executed command'
    post-deploy: npm install --prod && npm run build && npm run pm2
